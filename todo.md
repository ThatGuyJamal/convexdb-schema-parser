1. Generate function return types

This would require the developer explicity defining convex query and mutation return types so it can be parsed. 

2. Test if internal querys, mutations, and actions work
